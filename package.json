{
  "name": "ai-career-saas-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "start": "node server/server.js",
    "test": "cd server && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
